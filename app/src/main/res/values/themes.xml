<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application style -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Light theme -->
    <style name="AppTheme.Light" parent="AppTheme">
        <item name="android:windowBackground">@color/light_background</item>
        <item name="android:textColor">@color/light_text</item>
    </style>

    <!-- Dark theme -->
    <style name="AppTheme.Dark" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/dark_background</item>
        <item name="android:textColor">@color/dark_text</item>
    </style>

    <!-- Colorful theme -->
    <style name="AppTheme.Colorful" parent="AppTheme">
        <item name="colorPrimary">@color/colorful_primary</item>
        <item name="colorPrimaryDark">@color/colorful_primary</item>
        <item name="colorAccent">@color/colorful_accent</item>
        <item name="android:windowBackground">@color/colorful_background</item>
    </style>

    <!-- High Contrast theme -->
    <style name="AppTheme.HighContrast" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/high_contrast_text</item>
        <item name="colorPrimaryDark">@color/high_contrast_background</item>
        <item name="colorAccent">@color/high_contrast_correct</item>
        <item name="android:windowBackground">@color/high_contrast_background</item>
        <item name="android:textColor">@color/high_contrast_text</item>
    </style>
</resources>